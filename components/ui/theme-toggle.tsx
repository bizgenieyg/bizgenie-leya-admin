'use client';
import {useI18n} from '@/lib/i18n';
import {useTheme,type ThemeChoice} from '@/lib/theme';
const order:ThemeChoice[]=['system','light','dark'];
export default function ThemeToggle({compact=false}:{compact?:boolean}){const{t}=useI18n(),{theme,setTheme}=useTheme(),label=theme==='light'?t('themeLight'):theme==='dark'?t('themeDark'):t('themeSystem');if(compact)return <button type="button" className="compact-control" aria-label={`${t('themeLabel')}: ${label}`} title={`${t('themeLabel')}: ${label}`} onClick={()=>setTheme(order[(order.indexOf(theme)+1)%order.length]!)}><span aria-hidden="true">{theme==='light'?'☀':theme==='dark'?'☾':'◐'}</span></button>;return <label className="compact-select"><span className="sr-only">{t('themeLabel')}</span><select aria-label={t('themeLabel')} value={theme} onChange={e=>setTheme(e.target.value as ThemeChoice)}><option value="system">{t('themeSystem')}</option><option value="light">{t('themeLight')}</option><option value="dark">{t('themeDark')}</option></select></label>}
