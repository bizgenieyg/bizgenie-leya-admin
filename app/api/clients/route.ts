import {tenantBackend} from '@/lib/backend/tenant-proxy';
import {enrichParticipation} from '@/lib/backend/client-participation';
export const dynamic='force-dynamic';
export async function GET(request:Request){try{const auth=await tenantBackend(request);if('error'in auth)return auth.error;const incoming=new URL(request.url),query=new URLSearchParams();for(const key of ['page','limit','search','status']){const value=incoming.searchParams.get(key);if(value)query.set(key,value);}const response=await auth.call(`/api/admin/clients?${query}`),payload=await response.json();if(!response.ok)return Response.json(payload,{status:response.status});return Response.json({...payload,clients:await enrichParticipation(auth.db,auth.tenantId,payload.clients??[])});}catch{return Response.json({error:'Сервис временно недоступен.'},{status:502});}}
