import {tenantBackend} from '@/lib/backend/tenant-proxy';
import {enrichParticipation} from '@/lib/backend/client-participation';
export const dynamic='force-dynamic';
export async function GET(request:Request){try{const auth=await tenantBackend(request);if('error'in auth)return auth.error;const incoming=new URL(request.url),response=await auth.call(`/api/admin/clients?search=${encodeURIComponent(incoming.searchParams.get('search')??'')}`);if(!response.ok)return Response.json({error:'Не удалось загрузить клиентов.'},{status:response.status});const body=await response.json(),clients=Array.isArray(body)?body:body.clients??[];return Response.json({clients:await enrichParticipation(auth.db,auth.tenantId,clients)});}catch{return Response.json({error:'Сервис временно недоступен.'},{status:502});}}
